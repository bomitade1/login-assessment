
<?php

/**
 * Write a pseudo code, representing a login process, implement it using session in php
 * and also add a logout page. 
 */

// Pseudo code for a login process

// 1. Check if the user submitted the login form
      //if (isset($_POST['submit'])) {
// 2. Get the submitted username and password
      //$username = $_POST['username'];
      //$password = $_POST['password'];

// 3. Validate the username and password
      //if ($username === 'example' && $password === 'password') {
// 4. Start a session for the user
      //session_start();
      //$_SESSION['username'] = $username;

// 5. Redirect to the home page
      //header('Location: home.php');
      //  exit;
      //} else {
// 6. Show an error message
      // echo 'Invalid username or password.';
      //}
      //}


// 7. create a logout page:

